# Similarity Search Engine

Provides fast vector query lookup using cosine similarity metrics:

$$Similarity = \vec{A} \cdot \vec{B}$$

Since all catalog and query vectors are L2 normalized, similarity is calculated using a standard vector dot product.
