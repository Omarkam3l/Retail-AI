# API Module

This package defines the abstract controllers and request exceptions for the cloud-side FastAPI REST endpoints.

## Contents
* `interfaces.py` — Declares the `BaseAlertController` interface.
* `exceptions.py` — Defines HTTP validation and authentication errors.
