# Alerts Module

This package defines the interfaces, exceptions, and models for the Alert Engine, including alert thresholds, face blurring filters, and loop compilers.

## Contents
* `types.py` — Dataclass for `Alert`.
* `interfaces.py` — Declares the `BaseAlertEngine` interface.
* `exceptions.py` — Defines `AlertEngineError`.
