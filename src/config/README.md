# Config Module

This package handles parsing global application configurations from YAML files and environment variables, along with runtime feature flag controls.

## Contents
* `config.py` — Config data class schemas and loader methods.
* `feature_flags.py` — Static feature toggling helper class.
