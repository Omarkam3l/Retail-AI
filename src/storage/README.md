# Storage Module

This package defines the interfaces and exceptions for Edge and Cloud data persistence and caching models.

## Contents
* `interfaces.py` — Declares the `BaseAlertRepository` contract.
* `exceptions.py` — Defines database connection and query errors.
