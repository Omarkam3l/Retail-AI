# Detection Module

This package defines the abstract base interfaces and specific exception types for the Object Detection (YOLO) phase of the surveillance pipeline.

## Contents
* `interfaces.py` — Declares the `BaseDetector` interface defining the frame detection contract.
* `exceptions.py` — Defines `ObjectDetectionError`.
