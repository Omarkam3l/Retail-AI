# Dashboard Module

This package defines the interfaces and exceptions for the web administration dashboard rendering client actions and coordinate maps.

## Contents
* `interfaces.py` — Declares the `BaseDashboardRenderer` interface.
* `exceptions.py` — Defines `DashboardError`.
